namespace Compression {
    public class DataFile{
        private byte[] ByteArray;

        public byte[] GetBytes(uint start, uint length) {}
        
        public void LoadFromFile(string path) {}
    }
}